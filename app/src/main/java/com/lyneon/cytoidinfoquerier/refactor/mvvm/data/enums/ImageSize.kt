package com.lyneon.cytoidinfoquerier.refactor.mvvm.data.enums

enum class AvatarSize(val value: String) {
    ORIGINAL("original"),
    LARGE("large")
}

enum class BackgroundImageSize(val value: String) {
    THUMBNAIL("thumbnail"),
    ORIGINAL("original")
}